{
  "name": "selectorbeak",
  "version": "0.0.10",
  "description": "Selectors that provide a way to fetch data by themselves",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "npm run clean && tsc -p . --p tsconfig.test.json && jest --no-cache",
    "coverage": "npm run clean && tsc -p . --p tsconfig.test.json && jest --no-cache --coverage",
    "clean": "rm -rf dist coverage",
    "tsc": "tsc",
    "compile": "npm run clean && tsc -p .",
    "prepublishOnly": "npm run test && npm run compile"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/wernerdegroot/selectorbeak.git"
  },
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/wernerdegroot/selectorbeak/issues"
  },
  "homepage": "https://github.com/wernerdegroot/selectorbeak#readme",
  "dependencies": {},
  "devDependencies": {
    "@types/jest": "^24.0.11",
    "@types/node": "^11.11.2",
    "@types/react": "^16.8.8",
    "@types/react-redux": "^7.0.3",
    "jest": "^24.5.0",
    "typescript": "^3.3.3333"
  }
}
